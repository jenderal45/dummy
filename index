<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exmind Digital | Website Dalam Pengembangan</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', sans-serif;
        }

        body {
            min-height: 100vh;
            background: radial-gradient(circle at top, #1e3a8a, #020617);
            display: flex;
            justify-content: center;
            align-items: center;
            perspective: 1200px;
            color: #fff;
        }

        .scene {
            transform-style: preserve-3d;
        }

        .card-3d {
            width: 380px;
            padding: 40px 30px;
            background: linear-gradient(
                145deg,
                rgba(255,255,255,0.15),
                rgba(255,255,255,0.05)
            );
            backdrop-filter: blur(18px);
            border-radius: 24px;
            box-shadow:
                0 30px 60px rgba(0,0,0,0.4),
                inset 0 0 0 1px rgba(255,255,255,0.15);
            text-align: center;
            transform: rotateX(10deg) rotateY(-10deg);
            transition: transform 0.6s ease;
        }

        .card-3d:hover {
            transform: rotateX(0deg) rotateY(0deg) translateY(-10px);
        }

        .logo {
            width: 120px;
            margin-bottom: 25px;
            filter: drop-shadow(0 10px 20px rgba(0,0,0,0.6));
        }

        h1 {
            font-size: 18px;
            font-weight: 600;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        span {
            color: #38bdf8;
            font-weight: 700;
        }

        .subtitle {
            font-size: 14px;
            opacity: 0.8;
        }

        .glow {
            position: absolute;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(56,189,248,0.25), transparent 70%);
            filter: blur(80px);
            z-index: -1;
        }

        @media (max-width: 480px) {
            .card-3d {
                width: 90%;
            }
        }
    </style>
</head>
<body>

    <div class="glow"></div>

    <div class="scene">
        <div class="card-3d">
            <img src="exmind-logo.png" alt="Exmind Digital Logo" class="logo">

            <h1>
                Website ini sedang dalam pengembangan <br>
                oleh tim <span>Exmind Digital</span>
            </h1>

            <div class="subtitle">
                Kami sedang menyiapkan pengalaman digital terbaik untuk Anda
            </div>
        </div>
    </div>

</body>
</html>
